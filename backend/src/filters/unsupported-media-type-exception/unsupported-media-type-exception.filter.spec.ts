import { UnsupportedMediaTypeExceptionFilter } from './unsupported-media-type-exception.filter';

describe('UnsupportedMediaTypeExceptionFilter', () => {
  it('should be defined', () => {
    expect(new UnsupportedMediaTypeExceptionFilter()).toBeDefined();
  });
});
